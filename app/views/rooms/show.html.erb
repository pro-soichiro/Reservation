<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @room.name %>
</p>

<p>
  <strong>Place:</strong>
  <%= @room.place %>
</p>

<p>
  <strong>Number:</strong>
  <%= @room.number %>
</p>

<%= link_to 'Edit', edit_room_path(@room) %> |
<%= link_to 'Back', rooms_path %>

<p>
  <%= link_to '予約登録',new_room_entry_path(room_id: @room.id) %>
</p>

<% @room.entries.each do |entry| %>
  <%= render 'entries/index' ,entry: entry %>
<% end %>