<p>
  <strong>予約者名</strong>
  <%= entry.user_name %>

  <strong>Email</strong>
  <%= entry.user_email %>

  <strong>日付</strong>
  <%= entry.reserved_date %>

  <strong>時間</strong>
  <%= entry.usage_time %>

  <strong>人数</strong>
  <%= entry.people %>人

  <%= link_to '削除', entry_path(entry.id), method: :destroy, data: {confirm: "予約を削除してよろしいでしょうか？"}  %>
</p>
